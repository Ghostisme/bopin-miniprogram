# 本地变更记录

## 2026-09-02

### 视觉恢复

- 参考 `主播招聘/` 文件夹中的 414px 截图，恢复玫瑰粉品牌基准 `#e799b0`。
- 将小程序全局令牌从误用的蓝紫色 `#6C5CE7` 统一为粉色主色、冷灰背景、白色卡片和深墨色正文。
- TabBar、搜索、筛选、急招标签、浮动入口、简历弹窗、消息未读标识和详情页操作栏均使用同一套令牌。
- 将错误的 `1x1` 绿色 TabBar 占位图重建为 81x81 灰色/玫瑰粉 PNG 图标。
- 将 Claude 遗留的 `scripts/generate-icons.js` 旧入口转接到真实图标生成器，避免再次把图标覆盖成 1x1 占位图片。
- 减少大面积渐变，避免页面在不同手机上产生脏色和层级不清。
- 修复详情页推荐卡片跳转到不存在路径的问题。
- 补齐 H5 入口模板，并关闭与 Node 24 不兼容的 Taro 3.6 依赖预编译，恢复 H5 开发预览。

### 独立管理后台

- 管理后台已拆分为上级目录独立项目：`D:\github_code\bopin-admin`（React + Vite）和 `D:\github_code\bopin-admin-service`（Java + Spring Boot）。
- React 概览与通告审核已通过 Vite 代理连接 Java 接口，服务未启动时保留演示数据降级。
- 新增 `ADMIN-README.md`，记录两个工程的启动方式和前端代理关系。

### 客户吸引力与时间线功能落地

- 小程序首页新增“主播找工作 / 企业招主播”双入口、今日高匹配摘要、四个高频操作和重新排序的信息层级。
- 岗位卡片优先展示匹配度、薪资、企业认证与距离，减少用户判断成本。
- 合伙人提供的 V1.0 MVP、V2.0 成长期、V3.0 扩张期和 V4.0 全球化规划仅用于拆解业务范围，不进入管理后台菜单或业务接口。
- 管理后台的“业务运营”直接展示 12 项能力对应的真实订单、报名和处理记录；小程序服务中心负责用户侧办理这些能力。

### 变更说明

当前仓库在 Git 初始提交后一直使用本地工作区变更，没有可恢复的旧提交或编辑器备份。此记录将作为后续协作的视觉基线：除非明确提出改版，否则不要把主色改回紫色或绿色。
